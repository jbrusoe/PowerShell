## Using PowerShell to Connect to Databases

These files demonstrate how to use PowerShell to connect to both a SQLServer and MySQL database. Two methods are used: [Invoke-SQLCmd](https://docs.microsoft.com/en-us/powershell/module/sqlserver/invoke-sqlcmd?view=sqlserver-ps) as well as the .NET System.Data.SqlClient class.
